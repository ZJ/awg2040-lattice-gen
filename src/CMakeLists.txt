cmake_minimum_required(VERSION 2.6)
add_library(options "options.cpp" "options.hpp")
add_library(pulseTrain "pulseTrain.cpp" "pulseTrain.hpp")
add_library(awg "awg.cpp" "awg.hpp")
target_link_libraries(awg pulseTrain)
